// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url      = env("SETUP IN APP")
}

enum ROLES {
    admin
    user
}

enum IMAGE_RESOURCE_TYPES {
    product
}

enum PRODUCT_STATUSES {
    active // when a product is live, and batches are auctioning 
    inactive // before a product is live: default
    scheduled // when a product will go live automatically
    archived // when a product has been given up on
    sold // when all the quantities have been sold
}

model User {
    id        Int      @id @default(autoincrement())
    email     String   @unique
    role      ROLES    @default(user)
    name      String
    password  String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}

model Product {
    id_product        Int                 @id @default(autoincrement())
    sku               String?
    name              String
    slug              String
    priceCost         Float?              @db.Double
    priceInitial      Float?              @db.Double
    priceRetail       Float?              @db.Double
    initialQuantity   Int                 @default(1)
    remainingQuantity Int                 @default(1)
    quality           Int                 @default(5)
    auctionBatchCount Int                 @default(1)
    description       String?             @db.MediumText
    detailItems       Json                @default("[]")
    status            PRODUCT_STATUSES    @default(inactive)
    scheduledFor      DateTime?
    createdAt         DateTime?           @default(now())
    updatedAt         DateTime?           @updatedAt
    ProductTags       ProductTags[]
    ProductCategories ProductCategories[]
}

model Tag {
    id_tag      Int           @id @default(autoincrement())
    label       String
    slug        String
    ProductTags ProductTags[]
}

model Category {
    id_category       Int                 @id @default(autoincrement())
    label             String
    slug              String
    ProductCategories ProductCategories[]
}

model Image {
    id_image     Int                  @id @default(autoincrement())
    resourceType IMAGE_RESOURCE_TYPES
    resourceId   Int
    path         String
    alt          String

    @@index(fields: [resourceType, resourceId], name: "type_id")
}

model ProductTags {
    productId Int
    tagId     Int
    product   Product @relation(fields: [productId], references: [id_product])
    tag       Tag     @relation(fields: [tagId], references: [id_tag])

    @@id([tagId, productId])
}

model ProductCategories {
    productId  Int
    categoryId Int
    product    Product  @relation(fields: [productId], references: [id_product])
    category   Category @relation(fields: [categoryId], references: [id_category])

    @@id([productId, categoryId])
}
